<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Recipe</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" rel="stylesheet">
</head>

<body>
    <div class="container" style="margin-top: 120px;">
        <h2>Edit Recipe:
            <%= recipe.dishName %>
        </h2>

        <form action="/recipe/<%= recipe._id %>/update" method="POST">
            <% recipe.ingredients.forEach(function(ingredient, index) { %>
                <div class="input-field">
                    <input type="text" name="ingredientName[]" value="<%= ingredient.ingredientName %>" required>
                    <label for="ingredientName">Ingredient Name</label>
                </div>
                <div class="input-field">
                    <input type="text" name="quantity[]" value="<%= ingredient.quantity %>" required>
                    <label for="quantity">Quantity</label>
                </div>
                <% }); %>

                    <button type="submit" class="btn pink">Save Changes</button>
        </form>

    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script>
        // Initialize the select dropdowns in Materialize
        document.addEventListener('DOMContentLoaded', function() {
            let elems = document.querySelectorAll('select');
            M.FormSelect.init(elems);
        });
    </script>
</body>

</html>