<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Grocery Purchase List</title>
    <!-- Import Materialize CSS for layout styling -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" rel="stylesheet">
    <!-- Import Custom CSS -->
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/grocerypurchaselist.css">
</head>

<body>
    <!-- Navigation Bar -->
    <nav>
        <div class="nav-wrapper">
            <a href="/" class="brand-logo">FinGro</a>
            <ul id="nav-mobile" class="right hide-on-med-and-down">
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/contact">Contact</a></li>
                <li><a href="/login">Login</a></li>
            </ul>
        </div>
    </nav>

    <!-- Content Section for Grocery List -->
    <div class="container">
        <h2>Your Grocery Purchase List</h2>
        <p>Create and manage your grocery shopping list here.</p>

        <!-- Form for Number of People -->
        <form action="/update-grocery-list" method="POST" id="groceryForm">
            <div class="input-field">
                <label for="peopleCount">Number of People</label>
                <input type="number" id="peopleCount" name="peopleCount" value="1" min="1" required>
            </div>

            <!-- Hidden field for grocery list ID -->
            <input type="hidden" name="groceryListId" value="<%= groceryList._id %>">

            <!-- Submit Button -->
            <button type="submit" class="btn pink">Update Quantities</button>
        </form>

        <!-- Grocery List Table -->
        <table class="highlight">
            <thead>
                <tr>
                    <th>Ingredient Name</th>
                    <th>Total Quantity</th>
                </tr>
            </thead>
            <tbody id="groceryTableBody">
                <% if (groceryList.ingredients.length > 0) { %>
                    <% groceryList.ingredients.forEach(function(ingredient) { %>
                        <tr>
                            <td>
                                <%= ingredient.ingredientName %>
                            </td>
                            <td>
                                <!-- Display the updated total quantity -->
                                <%= ingredient.totalQuantity %>
                            </td>
                        </tr>
                        <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="2" class="center-align">No grocery items found.</td>
                                </tr>
                                <% } %>
            </tbody>
        </table>
    </div>
    <!-- End container -->

    <!-- Footer Section -->
    <footer class="page-footer pink">
        <div class="container">
            <div class="row">
                <div class="col s12 center-align">
                    <p class="white-text">Copyright © FinGro</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Import Materialize JS for components -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>